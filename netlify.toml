[build]
  functions = "netlify/functions"
  publish = "public"

[build.environment]
  NODE_VERSION = "18"

[functions]
  node_bundler = "esbuild"

# Sadece /api/* yönlendir - diğer her şey normal çalışsın
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200